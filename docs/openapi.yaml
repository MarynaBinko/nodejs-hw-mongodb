openapi: 3.1.0
info:
  version: "1.0.0"
  title: Students App
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description: An API for managing students and contacts.
tags:
  - name: Contacts
    description: Operations related to contacts
servers:
  - url: http://localhost:3001
  - url: https://nodejs-hw-mongodb-3lnf.onrender.com
paths:
  /contacts:
    get:
      $ref: "../swagger/paths/contacts/get.yaml"  # Corrected path
    post:
      $ref: "../swagger/paths/contacts/post.yaml"  # Corrected path
  /contacts/{id}:
    get:
      $ref: "../swagger/paths/contacts/{id}/get.yaml"  # Corrected path
    patch:
      $ref: "../swagger/paths/contacts/{id}/patch.yaml"  # Corrected path
    delete:
      $ref: "../swagger/paths/contacts/{id}/delete.yaml"  # Corrected path
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
  schemas:
    Contact:
      $ref: "../swagger/components/schemas/Contact.yaml"  # Corrected path
    ContactResponse:
      $ref: "../swagger/components/schemas/ContactResponse.yaml"  # Corrected path


